<%= govuk_summary_list do |summary_list| %>
  <% rows.each do |row| %>
    <% summary_list.row(
      key: row[:key],
      value: value(row),
      action: action(row),
      html_attributes: html_attributes(row),
    ) %>
  <% end %>
<% end %>
