<%= govuk_summary_list do |component| %>
  <% rows.each do |row| %>
    <%= component.slot(
      :row,
      key: row[:key],
      value: value(row),
      action: action(row),
    ) %>
  <% end %>
<% end %>
