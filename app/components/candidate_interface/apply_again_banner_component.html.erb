<%= render NotificationMessageComponent.new(:info,
                                            message: "#{all_withdrawn_copy} #{govuk_link_to('Do you want to apply again?', start_path, class: 'govuk-notification-banner__link')}",
                                            secondary_message: deadline_copy) %>
