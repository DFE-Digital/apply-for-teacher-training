<% if application_can_submit? %>
  <h2 class="govuk-heading-m">Review and submit your application</h2>
  <p class="govuk-body">
    You will have the opportunity to review your application in full before you submit it.
  </p>

  <div class="govuk-button-group">
    <%= govuk_link_to 'Review application', candidate_interface_continuous_applications_course_review_and_submit_path(@application_choice.id), class: 'govuk-button' %>

    <%= govuk_link_to('Save as draft', candidate_interface_continuous_applications_choices_path) %>
 </div>
<% else %>
  <% errors.each do |error| %>
    <% error.message.split(/\n+/).each_with_index do |line, index| %>
      <% if index.zero? && error.type == :immigration_status %>
        <%= govuk_warning_text do %>
          <%= line %>
        <% end %>
      <% else %>
        <p class="govuk-body"><%= line.html_safe %></p>
      <% end %>
    <% end %>
  <% end %>
<% end %>
