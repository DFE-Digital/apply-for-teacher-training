<% if action == :withdraw %>
  <p class="govuk-body">
    <%= govuk_link_to 'Withdraw this application', candidate_interface_withdrawal_reasons_level_one_reason_new_path(application_choice.id) %>
  </p>
<% elsif action == :respond_to_offer %>
  <p class="govuk-body">
    <%= govuk_link_to 'Respond to offer', candidate_interface_offer_path(application_choice.id) %>
  </p>
<% end %>
