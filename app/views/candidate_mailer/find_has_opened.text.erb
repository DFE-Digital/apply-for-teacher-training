<% if @application_form.first_name.present? %>
  Dear <%= @application_form.first_name %>
<% end %>

You can now find teacher training courses starting in the <%= @academic_year %> academic year.

[Find your courses](<%= I18n.t('find_teacher_training.production_url') %>)

You can start preparing your applications and submit them from <%= @apply_opens %>.

Training providers make offers throughout the year. Providers may close applications early if a course fills up.

Courses can fill up quickly, so apply as soon as you are ready. If a course closes, you will need to wait until next year to apply.

[Start preparing your applications](<%= sign_in_link %>).

Courses that sponsor visas may close earlier than other courses.

Training providers offer places on courses as people apply throughout the year. Courses stay open until they are full.  Courses that sponsor visas can close earlier than other courses.

[How the application process works](<%= candidate_interface_guidance_url %>).

<%= render 'gain_insights_into_life_as_a_teacher' %>

<%= render 'candidate_mailer/end_of_cycle/contact_us' %>

<%= render 'unsubscribe_from_emails_like_this' %>
