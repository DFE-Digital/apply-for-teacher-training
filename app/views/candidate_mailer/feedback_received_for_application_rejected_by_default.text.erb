Dear <%= @application_form.first_name %>

As you know, your application for <%= @course.name_and_code %> could not progress because <%= @course.provider.name %>
did not respond by their deadline of <%= @application_choice.rejected_at.to_fs(:govuk_date) %>.

They’ve now given you the following feedback:

<%= render 'candidate_mailer/application_rejected/reasons_for_rejection_intro' %>
<%= render 'candidate_mailer/application_rejected/reasons_for_rejection' %>

<% if @show_apply_again_guidance %>
  If this was useful, [use your feedback to strengthen your application and apply again](<%= application_choices_link %>).
<% end %>

<%= render 'get_help_teacher_training_adviser' %>
