Hello <%= @application_form.first_name %>

<% if @application_form.application_choices.count > 1 %>

  You have submitted an application for:

<% @application_form.application_choices.each do |application_choice| %>
  * <%= application_choice.course_option.course.provider.name %> - <%= application_choice.course_option.course.name %> (<%= application_choice.course_option.course.code %>)
<% end %>

<% else %>

  You have submitted an application for <%= @application_choice.course_option.course.name_and_code %> at <%= @application_choice.course_option.course.provider.name %>.

<% end %>

[Sign in to your account to check the progress of your application](<%= application_choices_link %>).

<% if @application_form.application_choices.count > 1 %>

  Your training providers will be in touch if they would like to organise an interview.

<% else %>

  Your training provider will contact you if they would like to organise an interview. Around the Easter and Christmas breaks, some providers could take longer to respond to applications.

<% end %>
