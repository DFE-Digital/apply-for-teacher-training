<% content_for :title, title_with_error_prefix(t("page_titles.#{@form.eu_nationality? ? 'immigration_status' : 'visa_or_immigration_status'}"), @form.errors.any?) %>

<% content_for :before_content, govuk_back_link_to(candidate_interface_personal_details_show_path, 'Back') %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_with model: @form, url: candidate_interface_edit_immigration_status_path, method: :patch do |f| %>
      <%= render partial: 'shared_form', locals: { f: f } %>
    <% end %>
  </div>
</div>
