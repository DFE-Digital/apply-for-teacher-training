<%= f.govuk_text_field(
  :start_year,
  label: { text: t('application_form.degree.start_year.label'), size: 'm' },
  type: :number,
  hint: { text: t('application_form.degree.start_year.hint_text', example_year: (Time.zone.today.year - 3)) },
  width: 4,
) %>

<%= f.govuk_submit t('save_and_continue') %>
