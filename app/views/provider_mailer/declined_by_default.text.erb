Dear <%= @provider_user.full_name || 'colleague' %>

# Offer declined by default

<%= @application_choice.application_form.full_name %> had 10 working days to respond to your offer to study <%= @application_choice.offered_course.name_and_code %>. They haven’t replied, so the offer has been automatically declined.
