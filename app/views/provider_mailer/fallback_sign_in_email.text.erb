Dear <%= @provider_user.full_name %>

You requested a link to sign in because DfE Sign-in is unavailable.

The link will expire in one hour. You can only use it once.

Sign in:

<%= provider_interface_authenticate_with_token_url(token: @token) %>
