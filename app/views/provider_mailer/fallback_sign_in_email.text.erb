Dear <%= @provider_user.full_name %>

You asked for a temporary sign in link because there’s a problem with DfE Sign-in.

Sign in:

<%= provider_interface_authenticate_with_token_url(token: @token) %>
