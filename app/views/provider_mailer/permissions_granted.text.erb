Dear <%= @provider_user.full_name %>

<% if @permissions_granted_by %>
  <%= @permissions_granted_by.full_name %> added you to <%= @provider.name %>. You can now manage their applications.
<% else %>
  You have been added to <%= @provider.name %>. You can now manage their teacher training applications.
<% end %>


You have permission to:
<% @permissions.each do |permission| %>
  - <%= I18n.t("user_permissions.#{permission}.description").downcase %>
<% end %>

You can sign in now if you have a DfE Sign-in account.

If you do not have a DfE Sign-in account, you should have received an email with instructions from dfe.signin@education.gov.uk.

Sign in:

<%= provider_interface_sign_in_url %>
