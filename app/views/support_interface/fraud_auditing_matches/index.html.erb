<%= render 'support_interface/candidates/candidates_navigation', title: 'Fraud auditing dashboard' %>

<p class="govuk-body govuk-!-width-two-thirds">Records appear in this list for applications that have a postcode, name and date of birth in common, but they were created using different candidate accounts.<p>
<% if @matches.empty? %>
  <p class="govuk-body">There are currently no duplicate applications matched on these criteria.<p>
<% else %>
  <%= render SupportInterface::FraudAuditingMatchesTableComponent.new(matches: @matches) %>
<% end %>
