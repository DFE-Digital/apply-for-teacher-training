<% content_for :title, 'Course options without vacancies' %>

<% content_for :before_content do %>
  <%= breadcrumbs({
    Performance: support_interface_performance_path,
    'Course options without vacancies': nil,
  }) %>
<% end %>

<%= render(SupportInterface::CourseChoicesTableComponent.new(course_options: @course_options)) %>

<%= render(PaginatorComponent.new(scope: @course_options)) %>
