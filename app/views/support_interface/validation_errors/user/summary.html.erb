<% content_for :title, 'Validation error summary' %>

<% content_for :before_content do %>
  <%= breadcrumbs({
    Performance: support_interface_performance_path,
    'Validation errors': support_interface_validation_errors_path,
    @user_type.to_s.humanize => [:support_interface, :validation_errors, @user_type],
    'Validation error summary': nil,
  }) %>
<% end %>

<%= render SupportInterface::ValidationErrorsSummaryComponent.new(validation_error_summary: @validation_error_summary, scoped_error_object: :form_object, source_name: @user_type, error_source: 'users', select_sort_options: select_sort_options) %>
