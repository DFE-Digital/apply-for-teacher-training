<% content_for :browser_title, "Are you sure you want to reject all applications awaiting references?" %>
<% content_for :before_content, govuk_back_link_to(support_interface_tasks_path) %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_with model: nil, url: support_interface_run_task_path('reject_applications_awaiting_references_at_end_of_cycle') do |f| %>
      <span class="govuk-caption-l">End-of-cycle</span>
      <h1 class="govuk-heading-l">Are you sure you want to reject all applications awaiting references?</h1>

      <div class="app-styled-content">
        <p>This task finds any applications that have been submitted but not sent to providers because they are awaiting references from the recently closed recruitment cycle, and moves them to the <code>application_not_sent</code> status.</p>
        <p>This should be run shortly after the Apply 2 deadline closes at midnight on <%= EndOfCycleTimetable.date(:apply_2_deadline).to_s(:govuk_date) %>.</p>
      </div>

      <%= f.submit 'Yes, I’m sure – reject all applications awaiting references',
        class: 'govuk-button govuk-button--warning',
        data: { module: 'govuk-button' }
      %>

      <p class="govuk-body">
        <%= govuk_link_to 'Cancel', support_interface_tasks_path %>
      </p>
    <% end %>
  </div>
</div>
