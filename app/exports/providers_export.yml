common_columns:
  - provider_name
  - provider_code

custom_columns:
  organisation_type:
    tpye: string
    format: string
    description: The type of provider. Possible values, lead_school, scitt, university
    example: lead_school

  agreement_accepted_at:
    type: string
    format: date-time
    description: Date the agreement was accepted
    example: 2020-11-01T00:00:00+00:00

  average_distance_to_site:
    type: number
    format: float
    description: The average distance to a provider site (in miles)
    example: 11.5
