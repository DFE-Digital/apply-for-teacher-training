default: &default
  store_options:
    max_age: <%= 7.days.to_i %>
    max_size: <%= 2.5.gigabytes %>
    namespace: <%= Rails.env %>
    expiry_method: :job
    expiry_queue: low_priority

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
