en:
  provider_interface:
    api_tokens:
      index:
        title: API tokens for %{provider_name}
        add_token: Create a token
        active_tokens: Active tokens
        revoked_tokens: Revoked tokens
        no_tokens: There are no API tokens for this organisation
        api_tokens: API tokens
        apply_api: Apply API (%{environment})
        what_tokens_are_for_html: Software vendors use API tokens to manage applications on other platforms via the %{link}.
        warning: API tokens give full access to all your organisation’s data. They must be handled securely, like passwords.
      new:
        title: Create an API token for %{provider_name}
        heading: Create an API token
        body: API tokens give full access to all your organisation’s data. They must be handled securely, like passwords.
        create_token: Clicking generate will create a token for %{provider_name}. It will be visible until you navigate away from the page.
        create: Create token
        apply_api: Apply API documentation (%{environment})
        token_description: Name
        description_hint: Enter a name for your token, such as who or what it is for
        token_for: Who is this token for?
        in_house: In-house developers
        third_party: A third-party software vendor
        select_vendor: Select or enter a vendor name
      show:
        title: API Token for %{token_name}
      confirm_revoke:
        title: Are you sure you want to revoke API Token for %{token_name}?
        token_for: API Token for %{vendor}
        warning: If you revoke this token, any software integrations using it will stop working immediately.
        revoke_token: Yes, revoke this API token
      revoke:
        success: You have revoked API Token for %{token_name}
      success:
        title: API token for %{provider_name}
        new_api_token_generated: Token created
        your_token_is_html: Your token is <br role="presentation"><code>%{token}</code>
        copy: Copy this token to your clipboard
        copy_success: Token copied to clipboard
        content: You cannot view this again. You should copy it and store it securely.
        warning: Treat this token like a password. It provides full access to your organisation’s data.
