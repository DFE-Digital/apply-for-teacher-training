en:
  activemodel:
    errors:
      models:
        support_interface/cycle_switcher_form:
          attributes:
            find_opens_at:
              blank: Enter a valid Find open date
              invalid_date: Enter a valid Find open date
            apply_opens_at:
              blank: Enter a valid Apply open date
              invalid_date: Enter a valid Apply open date
              apply_opens_after_find_opens: Enter an Apply open date that is after Find has opened
            apply_deadline_at:
              blank: Enter a valid Apply deadline date
              invalid_date: Enter a valid Apply deadline date
              apply_deadline_after_apply_opens: Enter an Apply deadline that is after Apply has opened
            reject_by_default_at:
              blank: Enter a valid reject by default date
              invalid_date: Enter a valid reject by default date
              reject_by_default_after_apply_deadline: Enter a reject by default date that is after the Apply deadline
            decline_by_default_at:
              blank: Enter a valid decline by default date
              invalid_date: Enter a valid decline by default date
              decline_by_default_after_reject_by_default: Enter a decline by default date that is after the reject by default date
            find_closes_at:
              blank: Enter a valid Find closes date
              invalid_date: Enter a valid Find closes date
              find_closes_after_decline_by_default: Enter a Find close date that is after the decline by default date
            winter_reject_by_default_at:
              blank: Enter a valid winter reject by default date
              invalid_date: Enter a valid winter reject by default date
              winter_reject_by_default_after_reject_by_default: Enter a winter reject by default date that is after the reject by default date
            winter_decline_by_default_at:
              blank: Enter a valid winter decline by default date
              invalid_date: Enter a valid winter decline by default date
              winter_decline_by_default_after_decline_by_default: Enter a winter decline by default date that is after the decline by default date
              winter_decline_by_default_after_winter_reject_by_default: Enter a winter decline by default date that is after the winter reject by default date
