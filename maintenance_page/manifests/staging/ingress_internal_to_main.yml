apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: apply-staging.test.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: apply-staging.test.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: apply-staging
            port:
              number: 80
